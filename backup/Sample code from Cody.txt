Put this code right before your closing ‘body’ tag. (</body>)

//Make sure html document is ready
$(document).ready(function(){
    //Check the browser to determine if touch support for leaflet-draw is needed
        if ( L.Browser.touch() ) {
               //Load scripts using jQuery
               $.ajax({
                              url: "static/dist/leaflet.draw.js",
                              dataType: "script",
                              cache: true
}).done(function() {
                              //If there is something you need to do after loading, do it here
});

            }
        else{
                  //Load other script using jQuery
               $.ajax({
                              url: "https://cdnjs.cloudflare.com/ajax/libs/leaflet.draw/0.2.3/leaflet.draw.js",
                              dataType: "script",
                              cache: true
}).done(function() {
                              //If there is something you need to do after loading, do it here
});
            }
});
